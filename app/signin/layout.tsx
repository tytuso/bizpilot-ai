import type { Metadata } from "next";

export const metadata:Metadata={title:"Sign in | BizPilot AI",description:"Sign in securely to access your BizPilot AI business workspace.",robots:{index:false,follow:false}};

export default function SignInLayout({children}:{children:React.ReactNode}){return children}
